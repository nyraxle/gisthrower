'use strict';

const base = require('./base.js');

const listAll = () => {
  return base.get('/gists');
};

const listStarred = () => {
  return base.get('/gists/starred');
};

module.exports = {
  all: listAll,
  starred: listStarred
};
